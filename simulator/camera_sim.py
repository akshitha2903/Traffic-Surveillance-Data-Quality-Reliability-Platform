"""
Camera simulator stub — fills in during Phase 1.

Plan: emit synthetic vehicle counts + heartbeats to the backend
on a configurable interval, simulating a fleet of cameras across
named Bengaluru locations (TC Palya, KR Puram, MVIT-Sadahalli, etc).
"""


def main() -> None:
    print("Camera simulator stub — Phase 1 implementation pending.")


if __name__ == "__main__":
    main()
